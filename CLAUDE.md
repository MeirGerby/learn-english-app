# Learn English App

A Hebrew-language English-vocabulary learning web app for a teacher, Hodaya Jerby. React 19 + TypeScript + Vite + Tailwind v4 + shadcn/ui, deployed to GitHub Pages (`https://meirgerby.github.io/learn-english-app/`) via `.github/workflows/deploy.yml` on every push to `main`. Firebase Auth (email/password) + Firestore (word bank, per-user stats/achievements, course content) - no backend service; everything talks to Firebase directly from the browser.

Read this file before starting work. It's kept intentionally short - detailed process philosophy and specs for not-yet-built features live in skills (linked below) so they don't bloat every session's context; load them only when actually relevant.

## Skills

- **`project-operating-system`** - multi-agent orchestration philosophy, task/verification lifecycle, autonomy levels. Load before large-scale architecture planning or coordinating multi-step/multi-agent work.
- **`vocabulary-game-and-research-agent`** - spec for two features that don't exist yet: a generic reusable vocabulary-game engine, and an autonomous research agent that proposes (never silently implements) new game ideas. Load only when actually building one of those.

## Structure

- `src/pages/HomePage.tsx` - Hodaya's bio + two buttons (games, course)
- `src/pages/games/GamesListPage.tsx` - the 8-game menu (responsive grid, up to 3/row) + achievements display
- `src/pages/games/{flashcards,scramble,fill-blank,listening,speed-round,word-match,type-word,sentence-builder}/` - one folder per game, each self-contained. Each has a dedicated subagent in `.claude/agents/game-*.md` - use it for changes scoped to that game. Band-exclusive games (`fill-blank`/`listening` = Band 3, `word-match`/`type-word`/`sentence-builder` = Band 2) show a locked full-page state below their required band instead of rendering.
- `src/pages/course/CoursePage.tsx` - login-gated; admin-only add/remove content (Firestore `courseContent`, video/image via URL - YouTube auto-embeds)
- `src/pages/auth/{LoginPage,RegisterPage}.tsx`, `src/pages/admin/AdminPage.tsx` (feedback log + one-time word-import tool)
- `src/pages/placement/PlacementTestPage.tsx` - 24-question mixed-band multiple-choice placement test; assigns a Band 1-3 and saves it to `userStats`
- `src/lib/{firebase,wordsDb,userStats,courseContent,placementTest}.ts`, `src/hooks/{useAuth,useGameScore,useAchievements}.ts` - shared logic
- `src/hooks/usePlacement.tsx` - `PlacementProvider` (mounted once in `App.tsx`, wraps the whole app) + `usePlacement()` hook reading from that shared context. Placement status is fetched once per signed-in session, not per-consumer - see rule 41 for why that matters.
- `src/data/wordData.ts` - offline/fallback word bank (Firestore `words` collection is the source of truth once seeded)
- `scripts/generate-vocab.mjs` / `scripts/integrate-vocab.mjs` - Phase 2 vocabulary-expansion tools (Gemini API generation + splicing into `wordData.ts`); see rule 19
- `.claude/agents/` - `game-flashcards`, `game-scramble`, `game-fill-blank`, `game-listening`, `game-speed-round`, `game-word-match`, `game-type-word`, `game-sentence-builder` (each scoped to its own game folder, reads shared code freely, doesn't edit other games or shared infra without flagging it), `product-advisor` (review-only, no write access - consult before non-trivial changes; direct first-principles feedback, not rubber-stamp praise)

## Learned Rules

Newer rules override older ones when they conflict. Categories: `[CODE]` `[STYLE]` `[ARCH]` `[TOOL]` `[PROCESS]` `[DATA]` `[UX]` `[DEPLOYMENT]` `[TESTING]` `[AGENTS]`.

<!-- New rules are appended below this line. -->

1. `[PROCESS]` Always initialize a git repo and push new projects to GitHub by default, without waiting to be asked.

2. `[PROCESS]` Always branch for new work/features, test, then merge to `main` - never commit feature work directly to `main`.

3. `[TOOL]` Always run/test via a real HTTP server (local dev server or the deployed site), never by opening files with `file://` URLs.

4. `[DEPLOYMENT]` Always open and verify the *deployed* site after finishing work - never rely on localhost or a successful `git push` as proof of deployment. Check the GitHub Actions "Build and deploy to GitHub Pages" run actually succeeded, and validate a distinctive string from the change against the live URL. If the workflow fails or gets stuck, diagnose and retrigger.

5. `[DATA]` Hodaya Jerby's bio facts (5 years teaching children, degree in English teaching) came directly from the user - don't invent or embellish credentials.

6. `[UX]` UI language is Hebrew, `dir="rtl"` throughout. English vocabulary words/example sentences stay in English (the content being taught).

7. `[DATA]` Each word entry uses a `translation` field: a direct Hebrew word/phrase translation (e.g. "Hello" → "שלום"), not a descriptive sentence. Used for flashcard backs, quiz/fill-blank answers, and scramble/listening hints.

8. `[ARCH]` Stack is React + Vite + TypeScript + Tailwind v4 + shadcn/ui (full migration completed 2026-08-18, replacing an earlier vanilla-JS version - see git history). TypeScript is mandatory project-wide, including where it adds ceremony a smaller project might skip - an explicit, confirmed user preference, not an oversight. **Not adopted**: Turborepo/monorepo structure (single Vite app - there's only one frontend and no backend, so `apps/`/`packages/` would add structure without a second consumer) and a NestJS/tRPC/PostgreSQL backend (explicitly deferred 2026-08-18 - Firebase already handles auth/data/hosting entirely client-side; do not build a backend speculatively, ask what it would actually own first).

9. `[PROCESS]` Verify each feature (typecheck + build, and actually exercising the change - dev server + a real interaction or headless-browser check, not just reading the code) before moving to the next.

10. `[AGENTS]` Prefer specialized, narrowly-scoped agents over one agent doing everything: the five `game-*` subagents own their own game folder and nothing else; `product-advisor` reviews and gives direct feedback but never implements. Delegate to them rather than editing another agent's scope directly.

11. `[TOOL]` "Use Google AI" / "Antigravity" for frontend design work means calling the Gemini API directly (`https://generativelanguage.googleapis.com/...`) with the user's AI Studio key - Google Antigravity itself is a separate IDE with no invocable API, it cannot actually be operated as a tool. Check `GET /v1beta/models` for what's currently available before picking a model (free-tier quota varies by model and has been zero for some preview models on this key).

12. `[DATA]` Word bank: Firestore `words` collection (public read, admin write) is the source of truth; `src/data/wordData.ts` is the offline fallback. `loadWords()` in `src/lib/wordsDb.ts` races Firestore against a 5s timeout rather than relying on try/catch alone - `getDocs`/`getDoc` can hang indefinitely rather than reject (seen with broken IndexedDB in this dev environment; plausible for some real users too). Seeded via the admin-only, idempotent "Import words" button in `src/pages/admin/AdminPage.tsx`.

13. `[DATA]` Per-user cumulative stats/achievements: Firestore `userStats/{uid}` (owner-only read/write), managed by `src/lib/userStats.ts`. Additive to, not a replacement for, the per-session localStorage score/streak shown in each game's header (works for anonymous users too). `recordAnswer`/`recordGameCompleted` are fire-and-forget (never `await`ed) so a stalled Firestore write never blocks gameplay.

14. `[UX]` Advanced-level content (`idioms`/`phrasalVerbs`/`advancedVocab` categories, and the Fill-in-Blank/Listening/Speed-Round games) carries the `LevelBadge` "רמה מתקדמת" tag. Speed Round is the one exception that draws from *every* category, not just advanced ones (explicit user request) - don't "fix" that to advanced-only.

15. `[UX]` `/course` requires login to view (redirects to `/login` otherwise) but is not admin-restricted to view - any signed-in user can see it. Only admins can add/remove content.

16. `[PROCESS]` A scheduled autonomous "product manager" cloud routine runs hourly (platform enforces a 1-hour minimum interval - true 15-minute cadence isn't possible) against this repo, fully unattended per explicit user confirmation. It's required to consult `product-advisor` and to actually test + verify deployment before shipping anything; shipping nothing in a given cycle is an acceptable outcome. See <https://claude.ai/code/routines> for the routine itself.

17. `[ARCH]` **Supersedes rule 14.** Replaced the old binary beginner/advanced split with a 3-tier Band system (`src/types.ts` `Band = 1 | 2 | 3`, Cambridge/Israeli-school convention explicitly requested by the user): Band 1 = basics/food/travel/foundation, Band 2 = business, Band 3 = idioms/phrasalVerbs/advancedVocab (`CATEGORY_BANDS` in `src/data/wordData.ts`). New users (signed in, non-admin) are redirected to `/placement-test` on first visit to any game route (`RequirePlacement` wrapping the game routes in `App.tsx`); a 24-question mixed-difficulty multiple-choice test assigns a band via `scoreToBand()`, saved to `userStats.placementBand`. `usePlacement()` derives `unlockedBand` (admins always see Band 3; anonymous visitors default to Band 1, untested and ungated - the mandatory test only applies once someone has an account, so open browsing isn't broken). Every game's `CategorySelect` shows locked bands as disabled with a 🔒 + band label; Fill-in-Blank and Listening are wholesale Band-3-only games and show a full-page locked state below that band. Speed Round still spans every band's categories (rule 14's original intent) but each category now individually respects the same lock. `LevelBadge` was renamed `BandBadge` and takes a `band` prop instead of always saying "advanced".

18. `[DATA]` Band 2 currently has only the `business` category (10 words) - it's the thinnest tier, an approximation using existing content rather than a real re-leveling of every word. The user has asked for a much larger vocabulary (~10,000 words/game) generated via the Gemini API and re-tagged by band; that content-expansion phase (Phase 2) hasn't started yet - Phase 1 (this entry) is the placement-test/gating architecture only, built to work against the current word bank so it doesn't block on content first. **Superseded by rule 20 - Phase 2 has since started.**

19. `[UX]` Achievement unlocks now surface as a toast instead of unlocking silently. `checkAchievements()` in `src/lib/userStats.ts` already computed `newlyUnlocked` but both callers (`recordAnswer`/`recordGameCompleted`, fire-and-forget per rule 13) discarded it - a student could unlock a badge mid-game and never find out except by later noticing it lit up on `GamesListPage`. Fixed via `window.dispatchEvent(new CustomEvent("achievement-unlocked", ...))` right after the Firestore write, with a new `src/components/AchievementToast.tsx` (mounted once in `App.tsx`, outside `<Routes>`, so it works on every page) listening for that event and rendering a small RTL toast per unlock, auto-dismissing after 4s. This is the first use of a DOM `CustomEvent` as a pub/sub mechanism anywhere in `src` - justified specifically because `recordAnswer`/`recordGameCompleted` are deliberately unawaited (rule 13), so there's no promise chain or prop path back to a mounted component; don't reach for a context provider or state library for this same problem. The toast component dedupes by achievement id for the tab's lifetime (in-memory `Set`) because `checkAchievements()` re-reads the stats doc on every call and can race - two overlapping `recordAnswer` calls can both see an achievement as not-yet-unlocked and both dispatch the same id.

20. `[DATA]` `[TOOL]` Phase 2 (content expansion) is underway: added ~1,200 Gemini-generated words via `scripts/generate-vocab.mjs` (generation, writes to gitignored `scripts/generated/`) + `scripts/integrate-vocab.mjs` (splices into `wordData.ts`, run with the target category names as args). New categories `technology` and `society` (Band 2) were added; `business`, `idioms`, `phrasalVerbs`, `advancedVocab` were expanded in place. Current per-band totals: Band 1 ≈625 (unchanged - `basics`/`food`/`travel` deliberately skipped this round, see below), Band 2 ≈684, Band 3 ≈605 - roughly balanced now, still well short of the ~10,000/band target, so this is an ongoing multi-session effort, not a one-shot. Notes for continuing it:
    - **Model quota**: the free-tier Gemini API key has *per-model* daily quotas (seen: `gemini-3.6-flash` capped at 20 req/day; `gemini-2.5-flash` is fully deprecated for this key - 404; `gemini-3.5-flash` worked as a fallback with its own separate quota). When one model's quota is exhausted mid-run, switch `GEMINI_MODEL` to another rather than waiting - check `GET /v1beta/models?key=...` for what's currently listed.
    - **`basics`/`food`/`travel` (Band 1) skipped this round**: generation requests for these collided heavily with the existing 595-word `foundation` list (both draw from the same common-word space), yielding as few as ~11 accepted words per 70-word batch. Either accept a higher batch-count/lower-yield cost when tackling Band 1, or scope the prompt more narrowly (e.g. explicitly excluding words a Cambridge Foundation Band I list would already cover) before retrying.
    - **Casing**: generated `advancedVocab` entries came back Capitalized (`"Epistemological"`); the existing convention there is lowercase, so `integrate-vocab.mjs`'s caller lowercased the first letter before integrating - check this per-category before integrating new batches, conventions aren't identical across categories (`basics`/`food`/`travel`/`business` are Capitalized, `foundation`/`idioms`/`phrasalVerbs`/`advancedVocab` are lowercase).
    - Content only lives in `wordData.ts` (the offline fallback) until someone re-runs the admin "Import words" button to reseed Firestore - the two can drift out of sync, same as rule 12 already notes.

21. `[UX]` Fixed a silent-blank-screen bug in Scramble, Fill-in-Blank, Listening, and Speed Round: each gated its whole play area on a truthy check (`order.length > 0` / `current = order[index]`) with no `else` branch, so a category whose word pool comes back empty after loading/filtering (a real risk given rule 20's per-category counts still vary a lot, and Fill-Blank's `blankOutWord` filter can strip a thin category to zero) rendered nothing below the category picker - no error, no message, just dead space. Added `src/components/EmptyGameState.tsx` (Hebrew RTL, icon + message + button back to `/games`, matching the existing `gameLocked` empty-state shape already used for band-gating) and render it in that branch instead across all four games. Flashcards wasn't touched - its empty-category path wasn't found to be broken. Shipped as two commits (Speed Round first, then the other three) after `product-advisor` approval; verified via headless-browser checks of both the normal-content path (regression) and the empty-state path (temporarily emptying a category / a disposable preview route, reverted before commit) - reading the diff alone wouldn't have caught a wrong guard condition. One of the game-specialist subagents (`game-speed-round`) left a `.slice(0, 0)` force-empty test hack uncommitted in its working tree after finishing (the committed version itself was already clean) - a reminder to always diff against `main` and rebuild before trusting a subagent's self-reported "done," even when its own tests passed.

22. `[UX]` `[AGENTS]` Fixed Word Scramble's "שחקו שוב" (Play Again) button doing nothing: it called `startWord(order[0])`, which only resets per-word UI state and never `showResults`, so the results screen never unmounted and the click had no visible effect - a dead-end with no way back into the game except switching category. Adopted the `roundKey` pattern already used by Listening/Fill-in-Blank/Speed Round (a state bumped by the button and added to the data-loading `useEffect`'s deps; dropped the old direct `startWord` call from the button entirely rather than leaving both). `product-advisor`-approved before implementing; verified with a full headless-browser round-trip (play to completion via skip, click Play Again, confirm the results heading disappears and a fresh "word 1 of N" round actually renders) rather than trusting the diff, per rule 21's own lesson. Process note this cycle surfaced: the `game-scramble` subagent was asked only to edit the file and run the build, but - since it wasn't given `isolation: "worktree"` - it shared the exact same live working directory and branch as the orchestrating session. It found the orchestrator's already-created commit sitting there and went ahead to merge it into `main` and push, on its own initiative, beyond what it was actually asked to do. The result happened to be correct here (same fix, verified independently after the fact), but it means an un-isolated subagent can act on shared git state beyond its literal instructions - don't assume "edit + build" stays scoped to just that when no worktree isolation is set.

23. `[TOOL]` This session's (and apparently the `game-scramble` subagent's) outbound network egress is policy-blocked to `meirgerby.github.io` and other generic public web hosts - both raw `curl` and the `WebFetch` tool return an explicit `EGRESS_BLOCKED`/403 for the live site, even though the GitHub MCP tools (API-based, not raw HTTPS) work fine for repo/commits/Actions data. So rule 4's "validate a distinctive string against the live URL" isn't literally possible from every session - when it isn't, verify via the GitHub Actions "Build and deploy to GitHub Pages" run's success/failure for the exact deployed commit SHA instead, and say so plainly rather than silently skipping the check or claiming a live-site check that didn't happen.

24. `[UX]` Added a submit-loading state to `LoginPage`/`RegisterPage`: `handleSubmit` previously awaited the Firebase `signInWithEmailAndPassword`/`createUserWithEmailAndPassword` call with zero visual feedback - the submit button stayed enabled with a static label the whole time. On school wifi/mobile data (this audience's actual devices), that reads as "nothing happened" and invites a second tap, which fires a duplicate auth request. Added an `isSubmitting` state: disables the button and swaps its label to "מתחברים..."/"נרשמים..." during the await, reset in a `finally`. `product-advisor` flagged that disabling the button alone doesn't stop an Enter-key-triggered duplicate submit (Enter fires the form's `onSubmit` directly, independent of the button's `disabled` attribute) - added an `if (isSubmitting) return;` guard at the top of `handleSubmit` itself to close that gap. Verified with a headless-browser (Playwright, installed ad hoc in scratch space - not a project dependency) test against `vite preview`: confirmed the button flips to disabled+loading-text immediately on click, and that three rapid clicks fire exactly one `identitytoolkit.googleapis.com` request. Note for future sessions: this dev/CI environment's Firebase calls hang indefinitely rather than resolving (consistent with rule 12's IndexedDB note) - don't expect an auth call to actually complete or error out here; verify the loading-state *transition*, not the end-to-end login itself.

25. `[UX]` `[ACCESSIBILITY]` Fixed the achievement badges on `GamesListPage` being effectively silent on mobile: each badge only exposed its unlock description (e.g. "צברת 50 תשובות נכונות בסך הכל") via the native `title` attribute, which never triggers on touch - and per rule 24's own note, mobile/school-wifi devices are this audience's actual hardware, not desktop mouse users. Every locked/unlocked badge was icon+name only with zero explanation for every logged-in user, every visit. Changed each badge from a `<span title=...>` to a `<button type="button" onClick={...}>` that toggles a per-badge `expandedAchievementId` state, revealing the description as a small `<p>` directly beneath that specific badge (not one fixed slot below the whole row, which `product-advisor` flagged would visually detach from the tapped badge once the row wraps) - `title` was kept too as a harmless desktop hover fallback, and `aria-expanded`/`aria-controls` were added for assistive tech. No new dependency: checked `src/components/ui/` first and there's no Popover/Tooltip primitive yet, and `product-advisor` agreed pulling in Radix for one info-reveal spot would be exactly the kind of speculative infra rule 8 warns against - a local toggle + conditional paragraph is the right weight for revealing one line of Hebrew text on tap. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview` at a mobile viewport, using a temporary hardcoded-`loggedIn`/fake-`stats` edit to `GamesListPage.tsx` (reverted before commit, same disposable-preview approach as rule 21) since real Firebase auth can't complete in this environment: confirmed the description is absent by default, appears on click with `aria-expanded="true"`, and disappears on a second click.

26. `[UX]` `[ACCESSIBILITY]` Fixed the Flashcards flip card being unreachable by keyboard: it was a plain `<div onClick=...>` with no `role`, `tabIndex`, or key handling, unlike the quiz-mode answer buttons in the same file which are real `<button>`s. Added `role="button"`, `tabIndex={0}`, `aria-pressed={flipped}`, an `onKeyDown` handler flipping on Enter/Space (`preventDefault()` on Space so the page doesn't scroll), and a `focus-visible:ring-2` ring matching the existing shadcn convention. Scoped entirely to `src/pages/games/flashcards/FlashcardsPage.tsx`, delegated to the `game-flashcards` subagent per rule 10. `product-advisor`-approved before implementing; verified with a real headless-browser (Playwright, scratch-installed, not a project dependency) keyboard pass against `vite preview` - confirmed Tab reaches the card, Enter and Space both toggle `aria-pressed`, Space does not scroll the page, and the focus ring class is present - not just a read of the diff.

27. `[UX]` Fixed a recurring retry-friction papercut in the two text-input games (Word Scramble, Listening Challenge): on a wrong-answer submit, `handleSubmit`'s failure branch showed red feedback but never touched the `<Input>` - the student's incorrect text just sat there, so every wrong guess required manually clearing it before retyping. On mobile (this audience's actual devices per rule 24) that's a fiddly select-and-backspace gesture, not a quick fix, and it happens on every single miss in both games. Fix: added one line, `inputRef.current?.select()`, to the existing wrong-answer `else` branch in both `src/pages/games/scramble/ScramblePage.tsx` and `src/pages/games/listening/ListeningPage.tsx` - selects the incorrect text so the next keystroke immediately overwrites it. No other logic touched (scoring, streak reset, and feedback text are unchanged in both). Delegated to `game-scramble` and `game-listening` per rule 10, run in parallel since the two files don't overlap. `product-advisor`-approved before implementing (verified the bug was real by reading both branches directly, not speculative). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview` for both games: typed a wrong answer, submitted, confirmed `selectionStart`/`selectionEnd` spanned the full input value, then confirmed a single keystroke replaced the entire value rather than appending to it. Listening is Band-3-gated, so testing it required a temporary hardcoded `gameLocked = false` override in `ListeningPage.tsx` (reverted before commit, same disposable-preview approach as rules 21/25) since anonymous sessions in this environment can't complete a real placement test.

28. `[UX]` Fixed `src/components/RequirePlacement.tsx` (wraps every game route per rule 17) rendering `return null` while `usePlacement()` was resolving - a blank white page for every logged-in student landing on a game route (bookmark, refresh, post-login redirect), not a one-frame flicker: per rule 12, the underlying `getStats()` Firestore read can take up to ~5s on real school wifi. Same silent-blank-state failure class as rules 21 and 24, just one render layer earlier than either of those touched. Fix: render the app's already-established `<TopBar />` + "בודק הרשאות..." loading pattern (copied verbatim from `AdminPage.tsx`) instead of `null` - no new component, no new visual language. Scoped to a single shared-infra file, implemented directly rather than delegated (it's not inside any one game's folder, so no `game-*` subagent owns it). `product-advisor`-approved before implementing. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: since anonymous auth resolves too fast in this app's own design to naturally observe the loading window (anonymous visitors are deliberately left ungated per rule 17), used the same disposable-preview technique as rules 21/25/27 - temporarily forced `usePlacement()`'s `loading` to `true`, screenshotted the result (confirmed TopBar + loading text, no blank screen), reverted the override, then re-verified the normal anonymous path still renders the games list directly with no regression - before committing only the real `RequirePlacement.tsx` change.

29. `[UX]` Fixed a no-confirm, unrecoverable delete on `src/pages/course/CoursePage.tsx`: the admin-only "מחיקה" button called `deleteCourseItem(item.id)` directly on click - one misclick by Hodaya (the sole admin) while managing course content permanently destroyed a video/image with zero undo path, since `deleteCourseItem` is an unconditional Firestore `deleteDoc`. Fix: added a `window.confirm()` guard naming the item's caption before calling delete, plus a try/catch around the now-awaited call that surfaces a Hebrew `alert()` on failure (`product-advisor` caught that the original proposal fired `deleteCourseItem` without awaiting or handling errors - the same silent-async-failure class already fixed elsewhere per rules 21/24 - so a flaky-wifi failure would leave the item undeleted with the admin believing otherwise). No new dependency: checked `src/components/ui/` and there's still no Dialog/AlertDialog primitive (same call as rule 25's tooltip decision) - native `confirm()`/`alert()` is the right weight for one destructive action on an admin-only page; their OS-chrome styling sits outside the app's `dir="rtl"` control same as everywhere else on the web, which is normal and not worth a custom modal to fix. Scoped to a single shared-infra file (not inside any game folder), implemented directly. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`, using the same disposable-preview technique as rules 21/25/27/28 (temporarily forced `admin = true` and a fake course item, since real Firebase auth can't complete in this environment): confirmed dismissing the confirm dialog leaves the item in place, and accepting it fires the delete call without crashing the page - before reverting the override and committing only the real `CoursePage.tsx` change.

30. `[UX]` Fixed the same no-confirm, unrecoverable delete pattern rule 29 fixed on `CoursePage.tsx`, but on its sibling in `src/pages/admin/AdminPage.tsx`: the admin-only feedback-list "מחיקה" button called `deleteDoc(doc(db, "feedback", item.id))` directly on click, missed in the rule 29 pass because it lives on a different page. Applied the identical pattern: `window.confirm()` naming a 40-char truncated snippet of the feedback text (feedback bodies run much longer than `CoursePage`'s captions, so the full text isn't shown in the dialog) before calling delete, plus a try/catch around the now-awaited call surfacing a Hebrew `alert()` on failure. No new dependency - same native confirm/alert reasoning as rule 29. `product-advisor`-approved before implementing (verdict: proceed, calling it a near-verbatim reuse of an already-approved pattern with low blast radius). Scoped to a single shared-infra file, implemented directly. Verified with a headless-browser (Playwright, scratch-installed via `npm install --no-save playwright` this cycle since the earlier ad-hoc install wasn't persisted between sessions) test against `vite preview` at `/learn-english-app/admin` (note: this app uses `BrowserRouter` with a `/learn-english-app` basename, not a hash router - navigate to the real path, not a `#/...` URL), using the same disposable-preview technique as rules 21/25/27/28/29 (temporarily forced `admin = true`, a fake `user`, and a fake feedback item): confirmed dismissing the confirm dialog leaves the item in place, the dialog text correctly truncates the snippet, and accepting it fires the delete call without crashing the page - before reverting the override and committing only the real `AdminPage.tsx` change.

31. `[UX]` Fixed the last unguarded async write left on `CoursePage.tsx`: the admin-only "add content" form's `handleAdd` awaited `addCourseItem` (a Firestore write) with no try/catch and no loading state, unlike every other async action on this same page (rule 29's delete button) or in the app (rule 24's Login/Register submit state). On flaky school wifi a failed or slow add left Hodaya staring at a form that visibly did nothing - no error, no disabled button, no way to tell if the video actually saved. Fix: added an `isSubmitting` state with an `if (isSubmitting) return;` guard at the top of `handleAdd` (rule 24's Enter-key lesson), wrapped the write in try/catch/finally (Hebrew `alert()` on failure, matching rule 29/30's wording style; fields only cleared on success so a failed submit doesn't lose what she typed), and disabled+relabeled ("מוסיפה...") the submit button while in flight - a mechanical reuse of two already-approved patterns, nothing novel. `product-advisor` approved with one trim: dropped a third proposed step (also disabling the video/image type-toggle buttons during submit) as scope creep unsupported by the actual failure mode in evidence, so only the submit button is gated. Scoped to a single shared-infra file, implemented directly (not inside any game folder). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`, using the same disposable-preview technique as rules 21/25/27/28/29/30 (temporarily forced `admin = true` and a fake `user`, plus a temporary local stand-in for `addCourseItem` gated by a `?testfail=1` query param so both the success and failure paths could be exercised without a real Firestore write, since rule 12's IndexedDB note means Firebase calls hang indefinitely in this environment): confirmed the button disables and relabels immediately on submit in both paths, the success path clears the fields with no alert, and the failure path shows the Hebrew alert while preserving the typed URL - before reverting the override and committing only the real `CoursePage.tsx` change. Found via a research subagent surveying the app end-to-end for gaps not already covered by rules 1-30 (ranked 4 candidates; this was #1 for smallest/safest/most mechanical).

32. `[UX]` `[ARCH]` Fixed a real content-gating bug (not just a UX papercut) in `FillBlankPage.tsx`/`ListeningPage.tsx`, the two wholesale Band-3-locked games from rule 17: both compute `gameLocked` from their own independent `usePlacement()` call, and `usePlacement()` (`src/hooks/usePlacement.ts`) has no caching across call sites - every mount fires its own fresh `getStats()` Firestore read (rule 12: up to ~5s on real wifi), even though `<RequirePlacement>` already wraps both routes and did its own separate `usePlacement()` call to get there. The old logic, `gameLocked = !placementLoading && unlockedBand < 3`, failed OPEN: during that second page-local fetch, `placementLoading` is `true`, forcing the whole expression `false` regardless of actual band - so a signed-in Band-1/Band-2 user who legitimately passed `RequirePlacement` (they've taken the test, just placed lower) would see the real Band-3 game - categories, live sentences/audio, fully interactive - for up to ~5s before it flipped locked out from under them. Fixed by flipping both to fail-closed: `gameLocked = placementLoading || unlockedBand < 3`. One line changed in each file, delegated to `game-fill-blank` and `game-listening` respectively per rule 10, run in parallel since the files don't overlap. `product-advisor`-approved before implementing, and explicitly confirmed the fix's scope was correctly limited to these two pages - Flashcards/Scramble/Speed Round call `usePlacement()` too but derive per-category unlocks directly from `unlockedBand` (which defaults to `1` while loading) with no `!loading` inversion, so they already fail closed by construction and don't share this bug. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: since real Firebase auth can't complete in this environment (rule 12) and `RequirePlacement`'s own gate would otherwise block reaching the game page at all, added a temporary `__testPlacement=<band>,<loading>` query-param override to `usePlacement.ts` (returning controllable fake `loading`/`unlockedBand` values) plus a matching temporary bypass in `RequirePlacement.tsx` (so it doesn't call the real hook and block on its own copy of the same fake loading state) - this let the game page's own race window be driven independently of the wrapper's. Confirmed band=1-mid-loading, band=1-settled, and band=3-regression cases all render correctly with the fix; then reverted only the fix lines (kept the test scaffolding) and reran the same three cases to confirm the *original* code actually leaks real game content in the mid-loading case, proving the test would have caught the regression - before restoring the fix and fully removing all disposable scaffolding (both temp query-param overrides, the scratch verification script) and committing only the two real one-line changes. The redundant per-page `getStats()` refetch that causes the race in the first place is a legitimate follow-up (give `usePlacement`/placement status a shared cache or context so it's fetched once per session instead of once per mounted consumer) but was deliberately left out of this fix's scope per product-advisor's explicit note not to fold a caching refactor into a one-line bug fix.

33. `[UX]` Closed the one gap rule 21 left open: `FlashcardsPage.tsx` never got the silent-blank-screen fix that Scramble/Fill-Blank/Listening/Speed-Round all received, because rule 21's pass concluded "its empty-category path wasn't found to be broken" - re-checked this cycle and that path is in fact broken, in both of the page's two modes. `mode === "flashcards" && card && (...)` (`card = words[fcIndex]`) and `mode === "quiz" && !showQuizResults && currentQuizWord && (...)` (`currentQuizWord = quizOrder[quizIndex]`) were both bare `&&` guards with no `else`, so a category whose word pool comes back empty (rule 20: per-category counts still vary a lot) rendered the category picker and mode-toggle buttons with dead space below - no error, no message - in flashcards mode and, independently, in quiz mode. Fixed by converting both guards to ternaries against the same shared `EmptyGameState` component the other four games already use, no new component. Delegated to `game-flashcards` per rule 10; `product-advisor`-approved before implementing (verdict: proceed, explicitly flagged to verify both modes independently since a partial fix covering only one would leave the other still blank). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`, using the same disposable-preview `?testempty=1` technique as rules 21/25/27/28/29/30/31/32 (forced `data = []` in the `loadWords(...).then(...)` callback): confirmed `EmptyGameState` renders in flashcards mode, confirmed it also renders after toggling to quiz mode on the same empty category, then reverted the override and confirmed a normal non-empty category still renders both modes correctly - before committing only the real 3-part fix (import + two ternaries).

35. `[UX]` Fixed the last unguarded async Firestore write in the app: `AdminPage.tsx`'s "מה כדאי לשפר?" feedback-submit form (`handleSubmitFeedback`) `await`ed `addDoc` with zero guard - no `isSubmitting` state, no try/catch, no disabled/loading button - the one instance of this exact pattern (rules 24, 29/30, 31) that survived every prior pass, including rule 30's own pass over this same file (which only fixed the delete button a few lines below this form). On flaky school wifi a failed write silently lost Hodaya's feedback text with zero error shown, and nothing stopped a duplicate `addDoc` from a fast double-click or double-Enter. Fix: mechanical reuse of rule 31's exact pattern - `isSubmittingFeedback` state, `if (isSubmittingFeedback) return;` guard at the top of the handler, `addDoc` wrapped in try/catch/finally (Hebrew `alert()` on failure matching rules 29/30's wording; text only cleared on success), submit button `disabled={isSubmittingFeedback}` with label swapping to "שומרת..." (feminine singular, matching `CoursePage.tsx`'s `handleAdd` convention from rule 31 since both are single-admin-only forms - not the plural "מתחברים"/"נרשמים" from rule 24, which addresses arbitrary students). No new dependency, single file, not inside any game folder - implemented directly. `product-advisor`-approved before implementing (verdict: proceed, verified the gap was real against source and confirmed the feminine-singular label choice matches precedent). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`, using the same disposable `?__testadmin=1[&testfail=1]` scaffolding technique as rule 31 (temporary admin/user override plus a stand-in for `addDoc` gated by a query param, since rule 12's IndexedDB note means real Firebase calls hang indefinitely here): confirmed the button disables and relabels immediately on submit, three rapid clicks don't crash or duplicate-submit, the success path clears the textarea and re-enables the button, and the failure path shows the Hebrew alert while preserving the typed text and re-enabling the button - before reverting all scaffolding and committing only the real 3-part `AdminPage.tsx` change. Found via the same research-subagent survey technique as rule 31 (ranked candidates against the full learned-rules history to avoid re-proposing something already fixed).

37. `[UX]` `[ACCESSIBILITY]` Extended the accessibility work from rules 25/26 to the app's two most frequent state-change moments, which were previously visual-only: per-answer right/wrong feedback in all 5 games, and the achievement-unlock toast (rule 19). Every game's feedback text (`ScramblePage.tsx`, `ListeningPage.tsx`, `SpeedRoundPage.tsx`, `FillBlankPage.tsx`, and `FlashcardsPage.tsx`'s quiz mode) was a `{feedback && <p>...}` pattern - the element didn't exist in the DOM until there was something to announce, and `AchievementToast.tsx`'s outer container had an `if (!toasts.length) return null;` guard doing the same thing. Screen readers reliably announce `aria-live` regions on a *content* mutation of an already-present node, not on a whole new populated node being inserted - so as written, a screen-reader user got zero signal on every single answer, in every game, plus zero signal on every achievement unlock. Fix (identical mechanical pattern across all 6 files): keep the element always mounted, add `aria-live="polite"` to it, and toggle only the text/color inside via optional chaining (`feedback?.text`, `feedback?.color`) instead of gating the element's existence. Reused the `min-h-*` Tailwind pattern already established for `ScramblePage.tsx`'s hint text (rule pre-dates this one) to prevent layout jump between empty and populated states. `product-advisor`-approved before implementing; also caught and corrected an early draft of this same proposal that would have kept the conditional-mount pattern and just added `aria-live` on top of it - which looks right in a diff but doesn't reliably work, exactly the kind of accessibility fix that's silently broken in practice. `polite` (not `assertive`) was deliberately chosen so per-answer announcements queue after current speech rather than interrupting it - `assertive` would be actively disruptive in a fast game like Speed Round. Delegated the 5 game files to their respective `game-*` subagents per rule 10, run in parallel since none of the files overlap; `AchievementToast.tsx` is shared infra (not inside any game folder) so it was edited directly. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: confirmed the feedback `<p>` in Scramble is present in the DOM (with `aria-live="polite"`, empty, no color class) before any answer is submitted, then confirmed that submitting a wrong answer updates that *same* node's text/color in place - element count on the page stayed identical before and after, proving no new node was inserted - which is the actual DOM behavior `aria-live` announcement depends on, not just the presence of the attribute. Did not verify with a real screen reader (NVDA/VoiceOver) since none is available in this environment - noting that honestly per rule 23's precedent rather than claiming a verification that didn't happen.

38. `[UX]` Fixed a wasted-effort trap on `PlacementTestPage.tsx` (`/placement-test` is the one game-adjacent route that's intentionally NOT wrapped in `RequirePlacement`, per rule 17 - it's what `RequirePlacement` redirects *to*, so it has to stay reachable without an account): an anonymous visitor could navigate there directly, answer all 24 questions in good faith, and only on the results screen learn "שימו לב: כדי שהרמה תישמר... יש להתחבר לפני ביצוע המבחן" - i.e. the one message that would have changed their behavior arrived only after the behavior was already over. Same failure class already fixed elsewhere (rules 21/28/33: warn before the time is spent, not after). Fix: added a second, shorter warning ("אתם לא מחוברים - התוצאה לא תישמר. מומלץ להתחבר לפני שמתחילים", with a `Link` to `/login`) rendered above the page's existing `loading ? ... : done ? ... : ...` conditional, gated on `!authLoading && !user && !done` - so it shows through both the "טוען שאלות..." loading state and the full question-answering flow, but not on the results screen (which keeps its own original message; showing both together there was tried and read as redundant, so the new banner is suppressed once `done`). The `!authLoading` gate (not just `!user`) avoids a one-frame flash for users who are actually logged in but whose auth state hasn't resolved yet yet, matching the fail-closed loading pattern from rules 28/32. Single file (`src/pages/placement/PlacementTestPage.tsx`), not inside any game folder, implemented directly. `product-advisor`-approved before implementing (verdict: proceed with changes - persistent-but-muted over a dismissible banner, since a dismiss-once design would just recreate the "arrived mid-tab, missed the moment" gap; render once above the whole conditional rather than duplicated per-branch). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: confirmed the banner appears (with a working `/login` link) while questions are still loading and while answering, played through all 24 questions to the results screen and confirmed the original results-screen message still shows while the new banner does not, i.e. no duplicate messaging - not just a read of the diff. Also surfaced and fixed a housekeeping issue while starting this cycle: the checked-out working tree's `origin/main` remote-tracking ref was 41 commits stale relative to actual GitHub (rules 19-33's history existed on the real `origin/main` all along; the local clone just hadn't fetched it) - a `git fetch` + fast-forward resolved it before any work began. No data was actually at risk, but worth noting since it briefly looked like a lost-work incident.

39. `[DATA]` `[TOOL]` Phase 2 round 2: the `basics`/`food`/`travel` collision problem from rule 20 is fixed - the generator was passing a generic "last 250 words inserted" sample as the exclusion list, which (due to insertion order) barely included any actual `foundation` words. Fixed by building a *targeted* exclusion list per category (the real, full word lists of the semantically related categories - e.g. `foundation`+`basics`+`food`+`travel` together for Band 1, ~750 words, easily within prompt budget) and shifting the prompt from "more A1 basics" to "A2, more specific/varied, still Band 1" so it isn't trying to re-derive words `foundation` already has. Yield went from ~11/70 to ~75/80 per batch. Added ~1,010 more words this round (basics/food/travel 150 each, business/technology/society 120 each, idioms/phrasalVerbs/advancedVocab 100 each) - total word bank now ~3,024 (Band 1 ≈1,075, Band 2 ≈1,044, Band 3 ≈905), still well short of 10,000/band but over 4x the original 715. Also found and fixed casing drift within `business` (original 10 entries Capitalized, round-1's 190 generated lowercase) - added a per-category `CASE_POLICY` (capitalize/lowercase/leave) to the integration step, normalized to whichever convention already has the majority of words in that category. 16 case-insensitive duplicate words exist across categories (e.g. `basics`'s "Hello" vs `foundation`'s "hello") - all pre-date this session (from before `foundation` was ever added), left as-is, not worth a cross-category rework right now. `scripts/generate-vocab.mjs`/`integrate-vocab.mjs` in the repo still reflect the round-1 (non-targeted-exclusion) approach - if continuing this again, rebuild the exclusion-list logic from this rule's description rather than reusing the committed scripts verbatim.

40. `[ARCH]` Fixed a real, pre-existing gameplay bug in `SpeedRoundPage.tsx` found during a live-site verification pass, not related to any recent change: the round ended after exactly one unanswered question instead of the intended `ROUND_SIZE` (12) whenever a student let the first question time out. Root cause: `startQuestion(nextOrder[0], words)` is called synchronously inside the data-loading `useEffect`'s `.then()`, before that state update has actually committed/re-rendered - so the `setTimeout` it schedules closes over a stale `handleTimeout`/`advance`, which itself closes over the pre-load `index`/`order` (`0`/`[]`). When that first timer fires, the stale `advance()` computes `next = 0 + 1 = 1 >= order.length (0)` and ends the round immediately. Confirmed via a real-time headless-browser trace (polling the live DOM every 2s, not `--virtual-time-budget` which doesn't reproduce real timer races) - reproduced the bug pre-fix (round ended at ~8s after question 1's 6s timeout), then confirmed the fix progresses correctly through multiple questions post-fix. Only Speed Round has this shape (a `setTimeout`-driven auto-advance scheduled from the initial load callback); Scramble/Fill-Blank/Listening only advance via user-triggered handlers attached to a definitely-fresh render, so they don't share this staleness risk - no equivalent fix needed there. Fix: a `advanceRef` ref, kept pointed at the latest render's `advance` via a no-deps `useEffect(() => { advanceRef.current = advance; })`, with both `handleTimeout` and `handleAnswer`'s advance-scheduling `setTimeout` calls routed through `() => advanceRef.current()` instead of calling `advance` directly - the standard "latest ref" pattern for keeping timer callbacks correct regardless of when they were scheduled.

43. `[ARCH]` `[UX]` Added 3 new Band-2 (intermediate)-exclusive games, following the exact same locked-below-band pattern rule 17 established for Fill-Blank/Listening at Band 3: **Word Match** (`word-match`, click-to-pair English↔Hebrew grid), **Type the Word** (`type-word`, shown the translation + a blanked context sentence, types the English word from memory - production/recall, unlike every other game which is recognition-only), **Sentence Builder** (`sentence-builder`, tokenizes a word's `example` sentence into shuffled click-to-place chips, reconstructs it in order). All three reuse the existing `WordEntry {word, translation, example}` shape - no new data fields needed - and the same `useGameScore`/`recordAnswer`/`recordGameCompleted`/`EmptyGameState`/`CategorySelect`/`BandBadge` infrastructure as every other game. Added a matching `intermediate_explorer` achievement (mirrors rule "advanced_explorer"'s pattern) and 3 new `GameKey`/`RoundsCompleted` entries. `GamesListPage` reworked from a single-column list to a responsive grid (`grid-cols-2 sm:grid-cols-3`, up to 3 per row on tablet/desktop, 2 on narrow phones - a literal always-3 grid was rejected as too cramped on real small-phone widths, which is this audience's actual hardware per rule 24) with more compact cards (icon + title + band badge only, description moved to the native `title` tooltip) and container widened from `max-w-xl` to `max-w-3xl` to give the grid room; games are now grouped by band in array order so the grid visually reads as a band-1/band-2/band-3 progression. All three new games verified via real browser interaction against a local server that mimics GitHub Pages' actual static-file behavior (not `vite preview`'s SPA fallback, which masks 404-redirect-trick bugs per rule 40's lesson) - Word Match brute-force-solved to completion, Type the Word's wrong-answer/hint/skip paths all exercised, Sentence Builder's chip placement/return/reset and wrong-order detection all exercised - using a temporary `?__testBand=N` override on `usePlacement()` (reverted before commit) since real Firebase auth can't complete in this dev environment (rule 12).

44. `[ARCH]` Fixed a real bug the user found in production, not from routine testing: completing the placement test and clicking "למשחקים" sometimes re-triggered the test instead of opening the games list. Root cause: every consumer of `usePlacement()` (`RequirePlacement`, `GamesListPage`, all 8 game pages) independently called `getStats()` on its own mount - no shared cache. Navigating from the just-completed `PlacementTestPage` to `/games` mounted a *fresh* `usePlacement()` call in `RequirePlacement` that raced its own Firestore read; if that read was slow or hit the 5s timeout (rule 12 - real risk on the school wifi this audience actually uses), it returned `stats: null`, `completedPlacement` came back `false`, and `RequirePlacement` correctly-per-its-own-logic (but wrongly overall) concluded the test had never been taken and redirected right back to it. This wasn't a one-line fix - it was the exact "legitimate follow-up" rule 32 had already flagged and deliberately left out of scope. Fix: converted `usePlacement.ts` into `usePlacement.tsx` exporting a `PlacementProvider` (mounted once in `App.tsx`, wrapping `<Routes>`) that fetches placement status *once* per signed-in session via context, plus an `applyPlacementResult(band)` function that `PlacementTestPage` calls the instant it computes the result - synchronously updating every mounted consumer before the Firestore write even resolves, so there's no window where a slow write can look like "test not taken." Verified by deliberately reproducing the bug first (a temporary `?__testAuth=1` override faking a signed-in user with no placement record, completing the test, clicking "למשחקים") - confirmed it re-triggered the test pre-fix, confirmed it correctly landed on `/games` with the right band post-fix. One test-scaffold trap worth recording: the first reproduction attempt used a fresh `{uid: "test-uid"}` object literal per render as the fake user, which itself caused the bug to "reproduce" via a *different* mechanism (a fresh object reference on every render kept retriggering the `useEffect([user])` that resets the band override) - switching to a stable module-level constant fixed the false positive and let the real fix be verified cleanly. Firestore is still the source of truth for returning sessions (logout/login on a different day) - this fix only removes the redundant *intra-session* refetch race, which was the user-reported symptom.

45. `[UX]` `[ACCESSIBILITY]` Closed the one gap rule 37's `aria-live` pass missed: `LoginPage.tsx` and `RegisterPage.tsx`'s error-message `<p>` (`className="text-destructive text-sm min-h-[18px] mt-1"`, holding wrong-password/email-in-use/password-mismatch text) was already always-mounted with reserved height and mutated in place via `setError()` - the correct shape per rule 37's own lesson about mount-vs-mutate - but rule 37 enumerated only the 5 games + `AchievementToast.tsx` and never touched the two auth forms, so a screen-reader user got zero signal on a failed login/registration despite these being the two forms every non-anonymous user must pass through. Fix: added `aria-live="polite"` to both existing `<p>` elements - purely additive, no markup/state/logic change, two files, one attribute each. `product-advisor`-approved before implementing (verdict: proceed, confirmed the shape genuinely matches rule 37's precondition for `aria-live` to actually fire). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: confirmed `aria-live="polite"` is present on both pages' error `<p>` before any submit, then on `RegisterPage` specifically (password-mismatch triggers a synchronous `setError` with no Firebase call, avoiding rule 12's indefinite-hang issue) confirmed the same node persists (element count unchanged) while its text mutates from empty to "הסיסמאות אינן תואמות." - the actual DOM behavior `aria-live` announcement depends on, per rule 37's own established verification method, not just a diff read. `LoginPage`'s real-error path wasn't exercised end-to-end since it requires an actual Firebase call that hangs in this environment (rule 12) - only the pre-submit attribute presence was confirmed there, noted honestly per rule 23's precedent.

41. `[UX]` `[ACCESSIBILITY]` Closed the one screen this app has with a mandatory quiz-style flow that rule 37's pass never reached: `PlacementTestPage.tsx`'s `handleAnswer`/options render conveyed correctness purely via button background color (`bg-green-100`/`bg-red-100`, no text anywhere) - a WCAG 1.4.1 color-only-cue failure on the one gate every signed-in student is required to pass through (rule 17), unlike every other quiz-style screen in the app (`FillBlankPage.tsx`, `SpeedRoundPage.tsx`, `FlashcardsPage.tsx` quiz mode), all of which got the `aria-live="polite"` feedback-paragraph treatment in rule 37. This page was excluded from that pass only because it's pre-placement infrastructure, not one of the 5 games - not because it was checked and found fine. Fix: added the identical always-mounted `<p aria-live="polite">` pattern (text/color toggled via `answered`, never conditionally mounted, per rule 37's own lesson about mount-vs-mutate) directly after the options `<div>`. One implementation difference from `FillBlankPage.tsx`'s version: since this page's options are Hebrew translations of an English prompt word (not English words), the wrong-answer message shows the correct Hebrew translation (`current.word.translation`, already present on the `PlacementQuestion` object with no lookup needed) rather than repeating the English word already shown in the header. Single file, not inside any game folder, implemented directly per rule 10's scoping (matches rules 28/38's precedent for this same file). `product-advisor`-approved before implementing; its one correction was dropping an unnecessary `.find()` lookup from the initial draft in favor of the already-present `current.word.translation` field. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: confirmed the feedback `<p>` is present-but-empty before any answer (element count unchanged after answering, proving the same node updates in place rather than a new one being inserted - the actual behavior `aria-live` depends on, per rule 37's precedent), then ran 8 trials clicking different option positions to independently confirm both the correct-answer path ("נכון! ✓", green) and the wrong-answer path ("לא נכון. התשובה הנכונה: ...", red, correct Hebrew translation shown) render with matching color/text in every case. Found via a research subagent surveying the app end-to-end for gaps not covered by rules 1-38 (ranked 3 candidates against the full learned-rules history; this was #1 for value given it sits on the app's one mandatory gate, ranked despite carrying slightly more implementation surface than a pure one-attribute change). This rule number was assigned after a concurrent-push conflict with another session's rules 39-40 (Phase 2 vocab round 2, Speed Round timer fix) - originally drafted as rule 39, renumbered to avoid the collision, no content otherwise changed.

42. `[UX]` Fixed a false "no content yet" flash on `CoursePage.tsx`: `items` initialized to `[]` and `subscribeToCourseContent`'s `onSnapshot` listener only calls back once Firestore's first real snapshot arrives (no synchronous/optimistic value), but the render logic was a bare `items.length === 0 ? <"no content"> : <content>` with no loading state for the content list itself - distinct from the page's existing `loading` from `useAuth()`, which only covers the auth check (already handled at the top of the file). Result: every visitor, student or Hodaya alike, briefly saw "עדיין אין תוכן בקורס. חזרו לבדוק בקרוב!" on every single page load, even when content exists, for the duration of the Firestore round-trip - the same loading-state-rendered-as-real-empty-state failure class already fixed one layer up in `RequirePlacement.tsx` (rule 28) and across all 5 games (rules 21/33), just never caught here. Fix: added a `contentLoading` state (`useState(true)`), set to `false` inside the `subscribeToCourseContent` callback wrapper (which also still calls `setItems`), and added a third leg to the existing ternary - `contentLoading ? "טוען תוכן..." : items.length === 0 ? "עדיין אין תוכן..." : <content>`. No change to `subscribeToCourseContent`'s shared signature in `courseContent.ts` - the wrapper stays local to this page, since that Firestore helper may have other future callers. `contentLoading` only ever gets set `false` (never back to `true`) so a later admin add/delete re-firing the same `onSnapshot` listener can't re-trigger a loading flash on an already-loaded page - `product-advisor` flagged this as the one thing to explicitly verify, not to change. Single file, shared infra (not inside any game folder), implemented directly. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`, using a temporary disposable `?__testcourse=<1|empty>` scaffold (fake logged-in admin user + a delayed fake `subscribeToCourseContent` emitting either one item or a genuinely-empty list after 1.5s, reverted before commit per the established pattern from rules 21/25/27/28/29/30/31/32/33/35): confirmed both cases show only "טוען תוכן..." during the delay (never the false empty message), then correctly settle into either the real content or the real empty-state message - before reverting all scaffolding and committing only the real 2-part `CoursePage.tsx` fix. Also fixed unrelated housekeeping at the start of this cycle: the checked-out working tree's `main` was a stale detached-HEAD checkout 55 commits behind actual `origin/main` (same class of issue rule 38 already hit once) - `git checkout main && git fetch && git pull --ff-only` resolved it before any work began.

46. `[ARCH]` Rule 44's `PlacementProvider` fix had its own bug, caught by the user in production immediately after: logging in and clicking "משחקים" got stuck forever on `RequirePlacement`'s "בודק הרשאות..." loading screen. Root cause: the fetch-once effect was keyed on `[user]` - the Firebase `User` *object* from `useAuth()`, not a primitive. `onAuthStateChanged` can (and in production, apparently did) fire more than once for the same signed-in session with a new object reference each time, even though the underlying identity hasn't changed. Each such firing re-ran the effect, which reset `statsLoading` back to `true` and restarted `getStats()` - if firings kept arriving faster than a fetch could resolve, `statsLoading` never had a chance to settle to `false`, so `loading` (and therefore `RequirePlacement`'s gate) stayed stuck indefinitely instead of just briefly. Fixed by keying the effect on `user?.uid` (a stable string, unaffected by object-reference churn) instead of `user` itself. Found and fixed the same latent pattern in two other places sharing the identical shape - `useAchievements.ts` (same stuck-loading risk, on `GamesListPage`'s achievements section) and `CoursePage.tsx` (lower risk since it's a live Firestore listener that would eventually still fire, but same fragile dependency) - both switched to `user?.uid` too. Verified without needing real multi-firing Firebase auth (impossible in this dev environment per rule 12): added a temporary scaffold in `usePlacement.tsx` simulating a new same-uid user object every 400ms via `setInterval` (6 firings over 2.4s) alongside an artificially-delayed 600ms fake stats fetch (so each firing's fetch would get cancelled by the next before resolving, under the old `[user]` dependency) - confirmed the `user?.uid`-keyed effect settles within ~1s and the page correctly proceeds past the loading gate despite firings continuing in the background for another 1.6s. Scaffold fully reverted before commit. General lesson for this codebase: never key a `useEffect` on a Firebase SDK object (`User`, likely also `DocumentSnapshot` etc.) - always extract the stable primitive identity (`.uid`, `.id`) first.

47. `[ARCH]` Rule 46 still wasn't the whole story - the user reported the exact same stuck "בודק הרשאות..." screen a third time, specifically "when a user is logged in" (their words), after a confirmed hard-refresh ruled out stale caching. Real root cause, finally: `getStats()` in `userStats.ts` raced `getDoc()` against a 5s timeout (per rule 12, to survive it *hanging*) but had no `try`/`catch` - if `getDoc()` *rejects* instead of hanging (a real Firestore permission or transient-network error, entirely plausible on the school wifi this audience uses), the timeout race doesn't help at all: `Promise.race` rejects immediately with that error, `getStats()` itself becomes a rejected promise, and `PlacementProvider`'s `getStats().then(...)` - with no `.catch()` - simply never fires, leaving `statsLoading` (and therefore the loading gate) stuck forever. This exactly matches "stuck when logged in": anonymous visitors never call `getStats()` at all (early-returns before touching Firestore), so only signed-in users could ever hit it. `loadWords()` in `wordsDb.ts` already had a `try`/`catch` around its identical timeout-race pattern (rule 12 undersold its own fix - the comment there only mentions the hanging case, not why the catch matters); `getStats()` was just missing the same protection. Fixed by wrapping `getStats()`'s body in `try`/`catch`, falling back to `null` on any rejection exactly like the timeout path already did. Also added a redundant `.catch()` at both call sites (`usePlacement.tsx`, `useAchievements.ts`) as defense in depth, so a future regression in `getStats()` (or anything else async these effects come to depend on) fails safe instead of silently hanging the loading gate again - this is now the third bug in this exact spot, so extra margin is warranted. Verified by simulating an actual *rejecting* fetch (not a hanging one, which is what rules 44/46's scaffolding tested) via a temporary scaffold - confirmed the fix settles the loading gate and proceeds correctly despite the rejection, reverted before commit. `checkAchievements()` (in `recordAnswer`/`recordGameCompleted`'s fire-and-forget path per rule 13) has the identical unguarded-`getDoc()` shape and is a known follow-up - lower urgency since nothing user-visible awaits it, but worth closing next time this file is touched.

48. `[UX]` `[ACCESSIBILITY]` Closed the last remaining gap in rule 37's `aria-live` pass: `SentenceBuilderPage.tsx` (one of the 3 games added in rule 43, after rule 37's pass had already run) still had the old conditional-mount feedback shape - two separate `{result === "incorrect" && <p>...}` / `{result === "correct" && <p>...}` blocks, so the correct/incorrect message `<p>` didn't exist in the DOM until an answer was submitted, meaning a screen reader got zero signal on every single attempt in this game. `TypeWordPage.tsx` (also added in rule 43) already had the correct always-mounted pattern independently; Sentence Builder was the one holdout. Fix: mechanical reuse of rule 37's exact pattern - a single always-mounted `<p aria-live="polite">` whose text/color/`dir` toggle via ternary on `result` (`"נכון! ✓"` green with no forced `dir` when correct; `` `המשפט הנכון: ${tokens.join(" ")}` `` red with `dir="ltr"` when incorrect, preserving the existing LTR-for-the-whole-paragraph behavior since the sentence is English; empty with a `min-h-[28px]` reserved height when unanswered, to avoid layout jump). No other state/logic touched. Delegated to `game-sentence-builder` per rule 10; `product-advisor`-approved before implementing. Verified two ways: (1) the specialist subagent's own headless-browser (Playwright) pass confirmed both the correct and incorrect paths render the right text/color/`dir` with the total `<p>` count on the page unchanged before/after (same node mutates, not a new one inserted - the actual behavior `aria-live` depends on, per rule 37's established method); (2) independently re-verified the incorrect path myself after the subagent finished, from a fresh build, using the same disposable `?__testBand=2` scaffold pattern rule 43 established. Process note worth recording: the subagent's first two scaffolding attempts to reach this Band-2-gated game for testing - editing shared `usePlacement.tsx` (out of its assigned `sentence-builder/` scope) and then an in-file `?__testUnlock=1` bypass inside `SentenceBuilderPage.tsx` itself - both leaked into the working tree and were caught and reverted by the orchestrating session before commit (once mid-run, once after the subagent's final report claimed a clean tree but the diff still contained a stray `gameLocked` bypass). Consistent with rule 22's caution about un-isolated subagents and rule 21's "always diff against `main` before trusting a subagent's self-reported done" - a subagent's own claim of a clean working tree should still be independently diffed before commit, not taken on faith.

49. `[UX]` `[ACCESSIBILITY]` Closed the one game rule 48's cleanup missed: `WordMatchPage.tsx` (the click-to-pair game added in rule 43) conveyed match/no-match purely through button background color (`bg-green-100` on a correct match, `bg-red-100` via the `wrongFlash` state on a wrong one) with zero `aria-live` region anywhere - the one game left out of rules 37/48's pass because it's a click-to-match mechanic rather than a text-submit/select-answer one, so it wasn't touched by either sweep. A WCAG 1.4.1 color-only-cue gap, same class as every prior game in this series. Fix: mechanical reuse of the now-standard pattern - a new `statusMessage` state (`{ text, color } | null`), set in `attemptMatch()` to `{ "התאמה נכונה! ✓", "text-green-600" }` on a correct match and `{ "לא נכון, נסו שוב", "text-red-600" }` on a wrong one (the wrong-match message is cleared back to `null` inside the same 600ms `setTimeout` that already resets `wrongFlash`, so the announcement disappears in sync with the visual reset rather than lingering stale), reset to `null` on category change/"שחקו שוב" alongside the other per-round state. Rendered as a single always-mounted `<p aria-live="polite">` near the "X מתוך Y זוגות" progress line, text/color mutated via optional chaining - not conditionally mounted, per rule 37's mount-vs-mutate lesson. Delegated to `game-word-match` per rule 10; `product-advisor`-approved before implementing (one flagged nuance: keep the wrong-match clear in step with `wrongFlash`'s own reset, which the implementation follows). Verified twice independently: the subagent's own headless-browser (Playwright) pass, then re-verified by the orchestrating session from a fresh build using a temporary `?__verifyBand=2` scaffold (reverted before merge) - confirmed the `<p>` is present-but-empty before any attempt, confirmed the total `<p>` count on the page (3) is identical before/during/after both a wrong match (text appears then clears ~600-700ms later) and a correct match (text appears and persists), i.e. the same DOM node mutates in place rather than a new one being inserted, which is what `aria-live` announcement actually depends on. Housekeeping note: this cycle's session started from a detached-HEAD checkout with `node_modules` never installed at all (not just stale, per rules 38/42's prior housekeeping notes) - `git checkout main && git fetch && git pull --ff-only` plus a fresh `npm install` resolved it before any work began.

50. `[UX]` Added a password-reset ("שכחתי סיסמה") flow to `LoginPage.tsx` - previously the app had zero self-service account-recovery path anywhere (grepped the whole `src` tree: no `sendPasswordResetEmail`, no "forgot"/"שכח" in any form), meaning a locked-out student/parent (real audience, per rule 24's own note about their hardware/wifi) had no way back in except contacting Hodaya directly, who also had no admin-side reset tool. Fix: an inline `mode: "login" | "reset"` toggle within the same file (not a new route - the reset form is one email field + one button, too small to justify its own page) that calls Firebase Auth's built-in `sendPasswordResetEmail(auth, email)` - zero new dependency, `firebase/auth` was already imported in this file for `signInWithEmailAndPassword`. Reuses every existing convention rather than inventing new ones: the `isSubmitting`-guard pattern from rule 24 (`resetSubmitting` + `if (resetSubmitting) return`), the `ERROR_MESSAGES` lookup table, and the always-mounted `aria-live="polite"` paragraph pattern from rule 45 (two of them here - one destructive-red for real errors, one green for the success message - both empty-but-mounted before submit, per rule 37's mount-vs-mutate lesson). The one genuinely new piece of logic: Firebase throws `auth/user-not-found` for a nonexistent email, which would leak account existence if shown as a distinct message - so that specific code is caught and mapped to the *same* generic success copy ("אם קיים חשבון עם אימייל זה, נשלח אליו קישור לאיפוס סיסמה") as a real send, while every other error code (`auth/too-many-requests` etc.) still falls through to a real, visible error. `product-advisor`-approved before implementing, with this exact branch flagged as "the load-bearing part of the whole feature." Scoped to a single shared-infra file (auth pages aren't inside any game folder), implemented directly. Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview`: confirmed the mode toggle renders/reverts correctly and the `aria-live` paragraphs are present-but-empty pre-submit with a stable `<p>` count (mount-vs-mutate, per rule 37's established method) - but since a real `sendPasswordResetEmail` call hangs indefinitely in this dev environment (rule 12's IndexedDB note applies here too), the three-way branch (real-account success / nonexistent-account / other-Firebase-error) couldn't be observed end-to-end through the real SDK call. Used a temporary `?__testReset=success|notfound|othererror` scaffold that short-circuited the real call with the corresponding mock outcome (same disposable-preview technique as rules 21/25/27/28/29/30/31/32/33/35/43/48) to independently confirm all three branches render the correct message/color - then fully reverted the scaffold (confirmed via `grep -r __testReset src/` returning nothing) and re-ran a full regression pass on a fresh build (normal login fields, register page, mode toggle) before committing only the real `LoginPage.tsx` change.

51. `[UX]` Added a `beginner_explorer` achievement (🔰, "חוקר/ת מתחיל/ה") to `ACHIEVEMENTS` in `src/lib/userStats.ts`, closing a gap in the completionist-badge pattern: `intermediate_explorer`/`advanced_explorer` already reward playing all 3 Band-2/Band-3 games each, but Band 1 (`quiz`/`scramble`/`speedRound` - the tier every beginner actually plays first) had no equivalent, a visible asymmetry on the achievement row every logged-in student sees on `GamesListPage`. Pure additive change - one new array literal, mirroring the other two entries' exact shape (`speedRound` deliberately double-counts toward both this and `advanced_explorer` since it already spans every band per rule 14, an established precedent, not a new decision) - no component/hook/type changes needed since `GamesListPage.tsx` and `AchievementToast.tsx` both already render `ACHIEVEMENTS` generically by array/id. Positioned before `advanced_explorer` (band-ascending order among the three explorer badges) per `product-advisor`'s note that the original draft's stated insertion rationale didn't match the array's actual pre-existing order. `product-advisor`-approved before implementing (verdict: proceed with changes - confirmed the mechanism end-to-end itself by reading `types.ts`/`GamesListPage.tsx`/`AchievementToast.tsx` directly, flagged the insertion-order and wording-consistency nits actually applied here, and judged the `speedRound` double-dip not worth blocking on since every explorer badge already only requires "played once," not mastery). Verified two ways: (1) a direct `tsx` script importing `ACHIEVEMENTS` and exercising `check()` against 5 different `roundsCompleted` combinations, confirming it only unlocks once all 3 Band-1 games have `>0` and confirming the array position/icon/text; (2) a headless-browser (Playwright, scratch-installed per rule 24's pattern) pass against a fresh `vite preview` build, using a temporary `?__testach=1` scaffold in `GamesListPage.tsx` (forcing `loggedIn`/`stats` to simulate the achievement already unlocked, reverted before commit, same disposable-preview technique as rules 21/25/27/28/29/30/31/32/33/35/43/48/50) confirming the badge renders with the correct icon/name, expands to show the correct Hebrew description on tap, and appears in the right order relative to the other two explorer badges - before reverting the scaffold (confirmed via `grep -r __testach src/` returning nothing) and committing only the real `userStats.ts` change.

52. `[UX]` Bumped the tap-target size of `SentenceBuilderPage.tsx`'s word-chip buttons (both the answer-slot chips and the bank chips) from `px-3 py-1.5` (~30-32px tall, no `min-h`) to `px-3.5 py-2.5 min-h-11` (~44px, the standard WCAG minimum) - a real gap against this app's own established convention, not a stylistic nitpick: Word Match's grid cells (`min-h-14`, 56px) and Flashcards/Fill-Blank/Speed-Round's answer buttons (`px-4 py-3.5`, ~52px) were all already sized well above this, leaving Sentence Builder's chips the smallest clickable targets in the app on the one game where a mis-tap has a real cost (placing a wrong chip either requires manually returning it, or once all slots fill, triggers the incorrect-answer path with a 2.2s delay before the next sentence, per rule 40-adjacent timing logic already in this file). Also bumped the bank container's placeholder `min-h-10` to `min-h-11` to match, avoiding a ~4px layout shift when the bank empties on the last chip placed. Purely a Tailwind class change across 3 lines, no logic/state/behavior touched. `product-advisor`-approved before implementing (verdict: proceed, confirmed `flex-wrap` with no `max-height`/`overflow:hidden` on either chip container means larger chips just add rows on narrow screens with no clipping risk, and endorsed 44px specifically over matching Word Match's 56px since this game's content is denser/more variable-width). Delegated to `game-sentence-builder` per rule 10; independently diffed against `main` before commit per rule 21/48's lesson (clean, matched the requested spec exactly - also caught and discarded an unrelated `package-lock.json` diff left over from a routine `npm install`, not part of this change). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern) test against `vite preview` at a 390px mobile viewport, using the same disposable `?__testBand=2` scaffold rule 43/48 established (added to `usePlacement.tsx`, reverted before commit, confirmed via `grep -r __testBand src/` returning nothing): confirmed chips render at 44px tall regardless of word length, no wrapping/clipping issues on a 14-token sentence, and a full click-to-place interaction (bank chip → answer slot, reset button appearing) still works correctly post-change.

53. `[ARCH]` Added a global error boundary (`src/components/ErrorBoundary.tsx`, a class component - React error boundaries have no hook equivalent, `componentDidCatch`/`getDerivedStateFromError` require a class) wrapping `<Routes>` in `App.tsx`, inside `PlacementProvider` but outside the route tree. Closes the one variant of this app's long-running "silent blank screen" failure class (rules 21, 28, 33, 38, 42) that was never addressed: all five prior fixes were loading-state races that self-corrected once data arrived - none of them touch what happens when a component actually *throws* while rendering (a malformed Firestore doc, a null-pointer edge case in any of the 8 games). Uncaught, React unmounts the entire tree, leaving a permanent blank page with no nav and no recovery path except a refresh the user has no reason to know to try. The fallback reuses `EmptyGameState.tsx`'s exact visual shape (icon + Hebrew heading + muted body text + button) rather than inventing new UI. `product-advisor`-approved before implementing (verdict: proceed), with one load-bearing correction: the recovery button must do a hard `window.location.href` reload rather than a soft `<Link>`/`navigate()`, because error-boundary state does not reset on route change by itself - a soft navigation would "return home" while the boundary was still in its caught-error state, silently re-rendering the same fallback and creating a second dead end layered on the first. Used `import.meta.env.BASE_URL` (resolves to `/learn-english-app/`) rather than a hardcoded path, matching `vite.config.ts`'s `base`. No error-tracking/reporting service added - out of scope per rule 8's "don't build speculatively," console-only for now. Verified by actually throwing a real error (not a mocked fallback render) via a temporary query-param-gated `throw` in `HomePage.tsx` (`?__testError=1`, same disposable-preview technique as rules 21/25/27/28/29/30/31/32/33/35/43/48/50/51) and driving it with a headless browser (Playwright, scratch-installed per rule 24's pattern) against `vite preview`: confirmed the normal page still renders with no regression, confirmed the fallback renders with the correct Hebrew heading/body/button on the thrown error, and - the actual point of product-advisor's caveat - confirmed clicking the recovery button lands on a genuinely re-rendered, working home page rather than the same fallback. Scaffold fully reverted and confirmed via `grep` before commit.

54. `[DEPLOYMENT]` Fixed `index.html`'s favicon link, which pointed at `/vite.svg` - a file that has never existed in `public/` or the build output (this app was never a default Vite template with that asset; `public/favicon.svg` is the real, unused, branded icon) - and even if it did exist, the absolute `/`-rooted href would never pick up `vite.config.ts`'s `/learn-english-app/` base path, so it would resolve to the wrong origin path regardless of GitHub Pages subpath hosting. Result: every browser tab, bookmark, and "add to home screen" icon showed a broken/generic icon instead of the app's actual branding. One-line fix - `href="/learn-english-app/favicon.svg"` - verified by inspecting the built `dist/index.html` (correct href) and confirming `favicon.svg` is present in `dist/` (Vite copies `public/` verbatim). Shipped as a separate commit from rule 53's error boundary in the same cycle per `product-advisor`'s note that the two are unrelated files/subsystems and shouldn't be bundled even when done together - low enough risk (`product-advisor`: "doesn't need the same verification rigor as the boundary") that a visual/build-output check was sufficient, no headless-browser pass needed.

55. `[ARCH]` Closed the one layer rule 53's error boundary didn't reach: `App.tsx`'s `<Routes>` had no `path="*"` catch-all, so any unmatched client-side path (stale bookmark, corrupted share link, typo, a since-removed route) rendered nothing at all - no `TopBar`, no message, no recovery path, same "silent blank screen" failure class as rules 21/28/33/38/42/53, just one layer up from all of them (a route that never matches, not a loading race or a thrown error). Genuinely reachable in production, not just a local quirk: the GitHub Pages SPA-redirect trick (`public/404.html` + the inline decode script in `index.html`) round-trips a real server 404 back into this exact client-side router. Fix: new `src/pages/NotFoundPage.tsx` (icon + Hebrew heading "הדף לא נמצא" + muted body text + `<TopBar />`, wrapped in the same `max-w-xl` container `HomePage.tsx`/`RequirePlacement.tsx`'s loading state already use - not reusing `EmptyGameState.tsx` directly since its copy is specific to "not enough words in this category," which would be misleading copy for a 404) plus one new `<Route path="*" element={<NotFoundPage />} />` in `App.tsx`. Deliberately does *not* copy rule 53's `window.location.href` hard-reload choice for its home button: that was specifically needed because `ErrorBoundary`'s own component state doesn't reset on soft navigation, which doesn't apply here - a `path="*"` match is a normal route change, so a plain `<Link to="/">` (matching `EmptyGameState.tsx`'s existing pattern) correctly unmounts `NotFoundPage` on click. `product-advisor`-approved before implementing (verdict: proceed with changes - corrected one factual detail in the proposal, that `ErrorBoundary`'s fallback does *not* itself render `TopBar`, but confirmed the conclusion to include `TopBar` in the new page was still right, citing `RequirePlacement`'s loading state as the actual precedent instead). Verified with a headless-browser (Playwright, scratch-installed per rule 24's pattern, run from inside the project directory since a script outside `node_modules`'s resolution scope can't `import` it) test against `vite preview`: direct navigation to an unmatched path renders `TopBar` + the correct Hebrew heading/message + a working home button; clicking that button lands on the real home page (bio text present); and a real existing route (`/games`) still renders normally with no 404 regression - all three checked in the same pass, not just a diff read.
